name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Prepare deployment files
        run: |
          # Debug: Show initial structure
          echo "Initial directory structure:"
          ls -la

          # Create config.js from secret
          echo "${{ secrets.CONFIG_JS }}" > js/config.js

          # Debug: Show final structure
          echo "Final directory structure:"
          ls -la
          echo "Contents of js directory:"
          ls -la js/

          # Verify all required files
          echo "Verifying files:"
          for file in index.html js/script.js js/config.js; do
            if [ -f "$file" ]; then
              echo "$file exists and size is: $(wc -c < "$file") bytes"
            else
              echo "WARNING: $file is missing!"
            fi
          done

          # Display config.js content length (without showing sensitive data)
          echo "Config.js content length:"
          wc -c < js/config.js

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
